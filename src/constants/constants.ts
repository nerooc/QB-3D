import * as THREE from 'three';

export const PLANE_SIZE = 100000;
export const LEFT_BOUND = (-PLANE_SIZE / 5) * 0.6;
export const RIGHT_BOUND = (PLANE_SIZE / 5) * 0.6;
export const CUBE_SIZE = 20;
export const CUBE_AMOUNT = 150000;
export const WALL_RADIUS = 40;

export const COLORS = {
  PURPLE: new THREE.Color(0x6942b8),
  PINK:  new THREE.Color(0xff2190)
};

